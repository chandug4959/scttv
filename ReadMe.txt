Hi, 
Before getting starts with the project execution. 
Prerequisites are Java development Kit 1.8. as to be installed and configured. Maven has to be configured in the machine. And Java HOME PATH and maven HOME PATH has to set. 
Import the project from GitHub. Do maven build. 
Run testNG. XML file. To get the results. Result is stored in test output file. In console. 
We have implemented. Page object model and have passed all the test cases in a single method.
Test Data sheet, Config files are place at src/test/resources location.
Common methods are place at src/main.java location 
